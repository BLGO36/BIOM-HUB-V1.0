local player = game.Players.LocalPlayer
-- 체크포인트 삭제 함수
local function clearCheckpoint()
    player:SetAttribute("Checkpoint", nil)
    print("스폰을 삭제했습니다.. -BIOM SCRIPT HUB")
end
-- clearCheckpoint 함수 호출 예시
clearCheckpoint()
